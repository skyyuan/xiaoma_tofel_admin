<%= content_for :head do %>
  <%= stylesheet_link_tag "grember" %>
<% end %>

<!-- right content -->
<div class="content W730">
  <!-- Nav tabs -->
  <%= render 'top_nav' %>

  <div class="tab-content">
    <div class="" id="voc_entry">
      <%= form_for @reproduction_question, url: reproduction_question_path(from: params[:from]), method: :put, html: {class: 'form-horizontal _reproduction_form', id: 'form6'} do |f| %>
        <%= render 'form', f: f %>
      <% end %>
    </div>
  </div>
</div>
