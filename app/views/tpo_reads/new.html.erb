<%= content_for :head do %>
  <%= stylesheet_link_tag "gtpo" %>
<% end %>

<!-- right content -->
<div class="content W730">
  <!-- Nav tabs -->
  <%= render 'top_nav' %>

  <span class="span1">&nbsp;<div id="unit"><%= @tpo_group_name %>, <%= TpoType::READ_TYPE[session[:_tofel_tpo_read_type].to_i] %></div></span>
  <span class="span2"><%= link_to '更改', choose_range_tpo_reads_path %></span>

  <div class="tab-content">
    <div class="" id="voc_entry">
      <%= form_tag tpo_reads_path, method: :post, class: 'form-horizontal _tpo_read_form', id: 'form6' do %>
        <%= render 'form' %>
      <% end %>
    </div>
  </div>
</div>
